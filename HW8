void dfs(int v, int graph[5][5], int visited[]) {
    visited[v] = 1;
    printf("%d ", v);

    for(int i = 0; i < 5; i++)
        if(graph[v][i] && !visited[i])
            dfs(i, graph, visited);
}
